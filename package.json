{
  "name": "ozk-customer-savings-goals",
  "private": true,
  "dependencies": {},
  "cacheDirectories": [
    "client/node_modules"
  ],
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "npm run build && npm run deploy"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "webpack": "^4.29.6"
  }
}
